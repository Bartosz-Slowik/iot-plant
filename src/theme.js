import { createTheme } from '@mui/material/styles';
const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
          main: '#1b691b',
        },
        secondary: {
          main: '#f50057',
        },
        text: {
          primary: '#0c2506',
        },
      },
})

export default theme